pipeline{
	agent any
	
	tools{
		maven 'Maven3'
	}
	
	stages{
		
		stage('Checkout'){
			steps{
				git branch:'master',
				url:'https://github.com/Sowmithsow/Milestone4.git'
			}
		}
		
		stage('Build'){
			steps{
                dir('Selenium_TestNG'){
				bat 'mvn clean compile'
			}
		}
}
		
		stage('Test'){
			steps{
              dir('Selenium_TestNG'){
				bat 'mvn test'
			}
		}
}
	}
}
